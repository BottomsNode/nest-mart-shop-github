export enum PRODUCT_STATUS {
  ACTIVE = 1,
  INACTIVE = 0,
}

export const PERMISSION_KEY = 'permissions';

export enum Pagination_Length {
  START = 1,
  VERY_SMALL = 2,
  SMALL = 5,
  MEDIUM = 10,
  LARGE = 20,
}

export const MAX_PROFILE_PICTURE_SIZE_IN_BYTES = 5 * 1024 * 1024;